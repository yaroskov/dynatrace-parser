from config.dict import errors_dict, services, paths


# User's configuration of program:

# Options:
options = {
	"sourcesNumber": 17,
	"printData": False,
	"writeData": True,
	"writeBeauty": True,
	"runFull": True,
	"customData": {"isOn": False, "date": "17.01.2022"}
}

# Services:
services = services.data

# Errors dictionary:
errors = errors_dict.errors

# Paths to program files and folders:
paths = paths.paths
